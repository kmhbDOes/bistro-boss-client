/**
 * Install stripe and react stripe js
 * Create a checkout form with card element
 * Create account on stripe and get publishable key
 * Get card information
 * Use test card to test payment
 * One the server install stripe : npm install --save stripe
 * Create a payment intent api with payment method types: ['card']
 * Make sure to provide amount in cents: amount = price*100
 * Call payment intent API to get client secret and store it in a state
 * Use confirmCartPayment API with client secret and card info
 * Display Confirm Card Error
 * Display confirm card success
 */